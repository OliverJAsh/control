graphic($background-image, $width, $height, $background-position = 0)
  background-image versionPath($background-image)
  background-position $background-position
  background-repeat no-repeat
  width $width
  height $height